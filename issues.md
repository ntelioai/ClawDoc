## new



## already filed
- Change the icon in the packaged macos app to the clawdoc logo (crab shuffling docs) not ntelio logo
- Using browser alerts works in the browser not in the packaged app
- move reindex into the settings dialog
- the diff view (History) is overflowing the dialog 
- the logo highlight is awkward, remove
- folder context menu: add "New .xlsx" and "New .docx" entries (on top of existing items)
- ability to close/hide the folder panel
- split view: display 2 tabs side by side with a draggable divider to control the split
- replace the separate "new file" / "new folder" icons with a single "+" icon that opens a menu: New Folder, New Markdown, New .xlsx, New .docx
- progressive collapse: clicking the collapse icon once collapses the focused folder; clicking again collapses its parent; again collapses the parent's parent, walking up until all subfolders within the focused mounted folder are closed
- all folder-related behaviour (context menus, collapse, +/new menu) must also apply in the two-pane (double folder) view
- group the settings into vertical tabs for clarity